// Design: Dedicated visitor path follows Harborview’s calm, paper-and-ink administrative direction; it keeps safety boundaries visible rather than hiding them behind a wizard.
import { VisitorRegistrationWorkspace } from "@/features/registration/VisitorRegistrationWorkspace";

export default function RegisterPage() {
  return <VisitorRegistrationWorkspace />;
}
